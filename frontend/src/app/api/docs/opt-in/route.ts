import { optIn } from "algoflow-sdk";

export async function GET(req: Request, context: any) {
    const res = await optIn("column adapt aerobic picture actual gravity case digital guess purpose exhaust rifle mix possible midnight car crime evil ritual whale mercy december wool above broom", 746497619)
    return Response.json({res: true});
}